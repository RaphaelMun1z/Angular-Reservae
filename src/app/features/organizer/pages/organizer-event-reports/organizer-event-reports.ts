import { Component, OnInit, inject } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { OrganizerStore } from '../../state/organizer.store';

@Component({ selector: 'app-organizer-event-reports', imports: [RouterLink], template: `
  <main class="organizer-page"><header class="page-header"><div><span class="eyebrow">Dados reais disponíveis</span><h1>Relatórios</h1><p class="muted">Métricas calculadas somente a partir de pedidos, ingressos e logs retornados.</p></div><a class="button secondary" [routerLink]="['/organizer/management/events', eventId]">Voltar</a></header>@if (store.loading()) {<p class="card">Carregando dados...</p>} @else if (store.error()) {<p class="notice">{{ store.error() }}</p>} @else if (!store.hasOrders() && !store.hasTickets() && !store.hasLogs()) {<p class="card">Ainda não há dados suficientes para gerar relatórios deste evento.</p>} @else {<section class="grid"><article class="stat"><span class="muted">Pedidos</span><strong>{{ store.orders().length }}</strong></article><article class="stat"><span class="muted">Ingressos</span><strong>{{ store.tickets().length }}</strong></article><article class="stat"><span class="muted">Logs de acesso</span><strong>{{ store.logs().length }}</strong></article><article class="stat"><span class="muted">Acessos permitidos</span><strong>{{ store.allowedCheckins() }}</strong></article></section><section class="card" style="margin-top:24px"><h2>Disponibilidade das fontes</h2><table><tbody><tr><td>Pedidos por evento</td><td>{{ store.ordersLoaded() ? 'Carregado' : 'Não carregado' }}</td></tr><tr><td>Ingressos por evento</td><td>{{ store.ticketsLoaded() ? 'Carregado' : 'Não carregado' }}</td></tr><tr><td>Logs de acesso</td><td>{{ store.logsLoaded() ? 'Carregado' : 'Não carregado' }}</td></tr></tbody></table><p class="muted">Receita e ocupação não são exibidas sem valores/capacidade confiáveis nos dados retornados.</p></section>}</main>
`, styleUrl: '../../organizer.scss', standalone: true })
export class OrganizerEventReports implements OnInit { private readonly route = inject(ActivatedRoute); protected readonly store = inject(OrganizerStore); protected readonly eventId = this.route.snapshot.paramMap.get('eventId') ?? ''; ngOnInit(): void { if (this.eventId) { this.store.loadOrders(this.eventId); this.store.loadTickets(this.eventId); this.store.loadLogs(this.eventId); } } }
